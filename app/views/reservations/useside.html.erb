<div class="backgroundposuta">
    <h1>Listing articles</h1>

    <h1>すべての投稿</h1>
     

     
    <table>
      <tr>
        <th>名前</th>
        <th>名前</th>
        <th>Text</th>
      </tr>
     
       <% @reservation.each do |reservation| %>
           
          <td><%= reservation.startdate %></td>
          <td><%= reservation.approval %></td>
          <td><%= link_to '詳細', ticket_path(@ticket) %></td>
        </tr>

        <% end %>
      
    </table>
</div>